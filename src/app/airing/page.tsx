import NoShowFound from "@/components/common/no-show-found";
import { FC } from "react";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <NoShowFound />;
};

export default Page;
